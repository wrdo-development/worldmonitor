# WRDO Cave — Railway build config

[phases.setup]
aptPkgs = ["curl"]

[variables]
NODE_OPTIONS = "--dns-result-order=ipv4first"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:wrdo"]

[start]
cmd = "node server-production.mjs"
